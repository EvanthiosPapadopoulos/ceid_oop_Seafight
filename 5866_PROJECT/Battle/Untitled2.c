//int tileType;

    //enum tiles {sea = 1, ship = 2, hit = 3, miss = 4 };
   // tiles x;
    //cout << x << endl;
    //cout << "Give me the type of the Tile." << endl;
   // cout << "you can gine only: sea, ship, hit, miss." << endl;
   // cin >> tileType;
    //if(tileType == 1)
   // {
//tiles x = sea;
 //   }

  //  cout << x << endl;

//    Tile firstTile;

 //   firstTile.setType(x);
 //   cout << firstTile.getType() << endl;
   // firstTile.draw(firstTile.getType());

